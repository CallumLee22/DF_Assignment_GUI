# Data Fundementals Assignment GUI

This is a GUI made to visualise the data solution created for the Data Fundementals assignment using Python.

## Setup
- Create a python virtual environment
- To install all required libraries run:
    ``` bash
    pip install -r requirements.txt
    ```
- To start the app, run:
    ```bash
    python app.py
    ```
